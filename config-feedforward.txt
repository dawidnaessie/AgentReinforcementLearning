[NEAT]
fitness_criterion     = max
fitness_threshold     = 100000
pop_size              = 40
reset_on_extinction   = False
no_fitness_termination = False

[DefaultGenome]
# Node activation options (tanh returns values from -1 to 1, ideal for movement vectors dx, dy)
activation_default      = tanh
activation_mutate_rate  = 0.05
activation_options      = tanh

# Node aggregation options
aggregation_default     = sum
aggregation_mutate_rate = 0.0
aggregation_options     = sum

# Node bias (optimized, subtle mutations)
bias_init_mean          = 0.0
bias_init_stdev         = 1.0
bias_init_type          = gaussian
bias_max_value          = 30.0
bias_min_value          = -30.0
bias_mutate_power       = 0.20
bias_mutate_rate        = 0.35
bias_replace_rate       = 0.01

# Node response / signal amplification
response_init_mean      = 1.0
response_init_stdev     = 0.0
response_init_type      = gaussian
response_max_value      = 30.0
response_min_value      = -30.0
response_mutate_power   = 0.0
response_mutate_rate    = 0.0
response_replace_rate   = 0.0

# Connection weights (subtle mutations to preserve learned skills)
weight_init_mean        = 0.0
weight_init_stdev       = 1.0
weight_init_type        = gaussian
weight_max_value        = 30.0
weight_min_value        = -30.0
weight_mutate_power     = 0.20
weight_mutate_rate      = 0.40
weight_replace_rate     = 0.01

# Enable/disable connections
enabled_default           = True
enabled_mutate_rate       = 0.01
enabled_rate_to_true_add  = 0.0
enabled_rate_to_false_add = 0.0

# Genetic compatibility (speciation and ecological niche protection)
compatibility_disjoint_coefficient = 1.0
compatibility_weight_coefficient   = 0.5

# Network architecture (Phase 10: Directional Altruism - 23 inputs, 2 action outputs)
# 23 normalized perceptual inputs:
# 1. Velocity VX (-1..1)
# 2. Velocity VY (-1..1)
# 3. Distance to nearest food #1 (0..1)
# 4. Direction vector to food #1 X (-1..1)
# 5. Direction vector to food #1 Y (-1..1)
# 6. Distance to 2nd nearest food #2 (0..1)
# 7. Direction vector to food #2 X (-1..1)
# 8. Direction vector to food #2 Y (-1..1)
# 9. Distance to nearest poison (0..1)
# 10. Direction vector to poison X (-1..1)
# 11. Direction vector to poison Y (-1..1)
# 12. Distance to nearest hazard (0..1)
# 13. Direction vector to hazard X (-1..1)
# 14. Direction vector to hazard Y (-1..1)
# 15. Distance to nearest enemy from another tribe (0..1)
# 16. Direction vector to enemy X (-1..1)
# 17. Direction vector to enemy Y (-1..1)
# 18. Critical ally direction X (-1..1, normalized direction to nearest starving ally < 20% energy)
# 19. Critical ally direction Y (-1..1, normalized direction to nearest starving ally < 20% energy)
# 20. Relative enemy velocity heading (-1..1, >0 retreating away, <0 charging)
# 21. Herd density of own tribe around agent (0..1, safety in numbers)
# 22. Distance to nearest wall/boundary (0..1)
# 23. Self energy level (0..1)
# Network architecture (Phase 10: Recurrent Neural Networks RNN)
# 23 sensory inputs, 2 action outputs (Accel X, Accel Y)
num_inputs              = 23
num_outputs             = 2
num_hidden              = 0
feed_forward            = False
initial_connection      = full_direct

# Topology mutations (optimized structural pruning to favor synaptic refinement)
conn_add_prob           = 0.08
conn_delete_prob        = 0.06
node_add_prob           = 0.015
node_delete_prob        = 0.025

[DefaultSpeciesSet]
compatibility_threshold = 3.8

[DefaultStagnation]
species_fitness_func = max
max_stagnation       = 20
species_elitism      = 2

[DefaultReproduction]
# Elitism at 4 ensures top 4 networks in population pass unchanged
elitism            = 4
survival_threshold = 0.2
min_species_size   = 2